# Task 5 Report — Rename SSE context/hook to Realtime

## Status: DONE

## Files moved
- `src/context/sse-context.tsx` → `src/context/realtime-context.tsx`
- `src/hooks/use-sse.tsx` → `src/hooks/use-realtime.tsx`

(jj correctly tracked both as renames, shown as `R` in `jj st`.)

## Symbol/import renames applied
Applied across all 22 files that referenced the old names (found via the initial grep):
- `src/index.tsx`
- `src/layouts/Header.tsx`, `src/layouts/Layout.tsx`, `src/layouts/SideDrawer.tsx`
- `src/context/app-context.tsx`, `src/context/realtime-context.tsx` (moved file)
- `src/hooks/use-realtime.tsx` (moved file)
- `src/components/installation/InstallationStatusCard.tsx`, `InstallationLogModal.tsx`
- `src/pages/Home/{BitcoinCard,ConnectionCard,LightningCard,HardwareCard,WalletCard,index}.tsx`
- `src/pages/Apps/{AppPage,AppInfo,AppStatusRefresh,index}.tsx`, `src/pages/Apps/customApps/Electrs.tsx`, `src/pages/Apps/__tests__/Apps.test.tsx`
- `src/pages/Settings/VersionBox.tsx`
- `src/utils/test-utils.tsx`

Renames performed (identifier-only, no value changes):
- `SSEContextType` → `RealtimeContextType`
- `sseContextDefault` → `realtimeContextDefault`
- `SSEContext` → `RealtimeContext`
- `SSE_URL` → `WS_URL` (string value unchanged: still `"/api/sse/subscribe"`)
- `SSEContextProvider` → `RealtimeProvider` (default export of context file)
- `useSSE` → `useRealtime` (default export of hook file)
- Import paths `@/context/sse-context` → `@/context/realtime-context`, `@/hooks/use-sse` → `@/hooks/use-realtime`
- Relative import paths (`./sse-context` in `app-context.tsx`, `./context/sse-context` in `index.tsx`) → updated equivalents

One relative import in `src/index.tsx` (`./context/sse-context`, not matching the `./sse-context` or `../context/sse-context` sed patterns used for the bulk pass) was missed by the initial scripted sed pass and caught by the post-rename grep check; fixed manually.

## Verification

### Grep confirmation (after fix) — no output, confirming no stale references remain:
```
grep -rn "sse-context\|use-sse\|SSEContext\|useSSE\|sseContextDefault\|SSE_URL\b\|SSEContextProvider\|SSEContextType" src --include="*.ts*"
```
Exit code 1 (no matches).

### `npm run tsc`
Passed with no errors (only npm's own "new version available" notice on stdout).

### `npm test`
```
Test Files  17 passed (17)
     Tests  86 passed | 1 skipped (87)
```
Same pass count as before the rename (pure rename, no behavior change).

## Commit
```
jj commit -m "refactor(web): rename SSE context/hook to Realtime

Transport-neutral rename ahead of the WebSocket swap. No behavior change.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
Commit id: `db8512cd` (jj change `mtuwpylk`)

## Concerns
None. `src/utils/test-utils.tsx` still has a local variable/prop named `sseProps` — this was intentionally left untouched since it's not one of the exact identifiers listed in the task's rename spec, and the task explicitly scoped renames to the listed symbols only.

Note: `web/CLAUDE.md` still documents the old `SSEContext`/`useSSE`/`sse-context.tsx` names (out of scope for this task per instructions — only `src/` renames were requested).
