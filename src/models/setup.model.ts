export enum SetupSteps {
  WELCOME = 0,
  SETUP_FRESH,
  UPDATE,
  RECOVER,
  MYNODE_MIGRATION,
  UMBREL_MIGRATION,
  SET_PASSWORDS,
  PROVISION,
  WALLET_CREATION,
  DONE,
}

export enum SetupStatus {
  DONE = "done",
}
