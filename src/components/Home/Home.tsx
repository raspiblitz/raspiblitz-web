import React, { Component } from 'react';

export class Home extends Component {
  render(): JSX.Element {
    return (
      <React.Fragment>
        <div>Dashboard</div>
      </React.Fragment>
    );
  }
}

export default Home;
