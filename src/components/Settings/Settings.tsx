import React, { Component } from 'react';

export class Settings extends Component {
  render(): JSX.Element {
    return (
      <React.Fragment>
        <div className='content-container w-full bg-gray-300 dark:bg-gray-600 dark:text-white'>Settings</div>
      </React.Fragment>
    );
  }
}

export default Settings;
