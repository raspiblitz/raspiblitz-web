import { FC } from 'react';
import SetupContainer from '../../../container/SetupContainer/SetupContainer';

const Provision: FC = (props) => {
  return <SetupContainer>Provision</SetupContainer>;
};

export default Provision;
